{
    "version": 2,
    "framework": "vite",
    "buildCommand": "pnpm --filter web-back-office build",
    "outputDirectory": "dist",
    "installCommand": "pnpm install --frozen-lockfile",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}