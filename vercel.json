{
    "version": 2,
    "buildCommand": "pnpm --filter web-back-office build && cp -r apps/web-back-office/dist ./dist",
    "outputDirectory": "dist",
    "installCommand": "pnpm install --frozen-lockfile",
    "framework": null,
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}