version: '3.8'

services:
  # NOTE: Local postgres removed — database is hosted on Supabase
  # Connection string: aws-0-eu-west-1.pooler.supabase.com (session pooler, port 5432)

  redis:
    image: redis:7-alpine
    restart: always
    ports:
      - "6379:6379"

  backend:
    image: demegakitchen/demegapos-backend:latest
    restart: always
    environment:
      - DATABASE_URL=postgresql://postgres.iebuntdyiynwcyeyhdey:Admin-admin1%23@aws-0-eu-west-1.pooler.supabase.com:6543/postgres?pgbouncer=true
      - REDIS_URL=redis://redis:6379
      - JWT_SECRET=c4800476e4470153535d86f2f3dc83d4582e62c3dc8f322718456742b286ed4a
      - PORT=3000
      - FRONTEND_URL=http://localhost:8080
      - MAIL_PROVIDER=RESEND
      - SMTP_HOST=smtp.resend.com
      - SMTP_PORT=587
      - SMTP_SECURE=false
      - SMTP_USER=resend
      - SMTP_PASS=re_NStgbNAb_GQFQp2W8bDvoK3TQTEVi3mkf
      - SMTP_FROM=DeMegaPOS <onboarding@resend.dev>
      - FALLBACK_SMTP_HOST=smtp.gmail.com
      - FALLBACK_SMTP_PORT=587
      - FALLBACK_SMTP_SECURE=false
      - FALLBACK_SMTP_USER=demegakitchen5@gmail.com
      - FALLBACK_SMTP_PASS=oktz xxic huwu ugyf
      - FALLBACK_SMTP_FROM=DeMegaPOS <demegakitchen5@gmail.com>
    depends_on:
      - redis
    ports:
      - "3000:3000"

  web-back-office:
    image: demegakitchen/demegapos-web-back-office:latest
    restart: always
    ports:
      - "8080:80"
    depends_on:
      - backend

  web-kitchen:
    image: demegakitchen/demegapos-web-kitchen:latest
    restart: always
    ports:
      - "8081:80"
    depends_on:
      - backend

  web-customer:
    image: demegakitchen/demegapos-web-customer:latest
    restart: always
    ports:
      - "8082:80"
    depends_on:
      - backend

  prometheus:
    image: prom/prometheus:latest
    restart: always
    ports:
      - "9090:9090"
    volumes:
      - ./infra/prometheus.yml:/etc/prometheus/prometheus.yml

  grafana:
    image: grafana/grafana:latest
    restart: always
    ports:
      - "3001:3000"
    depends_on:
      - prometheus

# No local volumes needed — using Supabase managed database
